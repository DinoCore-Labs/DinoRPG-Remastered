import { type Config } from 'prettier';

const config: Config = {
	plugins: [],
	printWidth: 120,
	semi: true,
	singleQuote: true,
	useTabs: true,
	trailingComma: 'none',
	bracketSpacing: true,
	arrowParens: 'avoid'
};

export default config;
